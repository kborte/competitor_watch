__pycache__/
*.pyc
.DS_Store

# One .env at the repo root now configures every component; .env.example is the
# committed template. The old per-package files are gone.
.env
backend/.env
research_crawler/.env

*-key.json
