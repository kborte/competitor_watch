# Local development: the backend runs on its own port, so this is absolute.
# In the cluster it is the relative prefix "/api" — the dashboard and the API
# share one origin behind the Ingress, which is what lets one image serve every
# environment.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
