[pytest]
testpaths = tests
# The crawler and the backend are separate deployables that live in one repo;
# both are importable from the root, which is where pytest runs.
pythonpath = .
filterwarnings = error::DeprecationWarning
